name: Build & Release (Bun single executable)

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  build-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install deps
        run: bun install --no-save

      - name: Build all targets
        run: bun run build:all

      - name: Make macOS binaries executable
        run: |
          chmod +x dist/fastly-scanner-macos-x64 || true
          chmod +x dist/fastly-scanner-macos-arm64 || true

      - name: Package macOS as tar.gz
        run: |
          cd dist
          tar -czf fastly-scanner-macos-x64.tar.gz fastly-scanner-macos-x64
          tar -czf fastly-scanner-macos-arm64.tar.gz fastly-scanner-macos-arm64

      - name: Generate checksums
        run: |
          cd dist
          (sha256sum * || shasum -a 256 *) > sha256sums.txt

      - name: List artifacts
        run: ls -lh dist

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/fastly-scanner-linux-x64
            dist/fastly-scanner-linux-arm64
            dist/fastly-scanner-windows-x64.exe
            dist/fastly-scanner-windows-arm64.exe
            dist/fastly-scanner-macos-x64.tar.gz
            dist/fastly-scanner-macos-arm64.tar.gz
            dist/sha256sums.txt
          draft: false
          prerelease: false
